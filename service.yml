apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: <your_namespace_previously_created>
spec:
  selector:
    app: webapp
  ports:
  - name: http
    port: 80
    targetPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: <your_namespace_previously_created>
spec:
  selector:
    app: redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379
